import React from 'react'
import { css } from '@emotion/core'

const Layout = css`
    height: 100%;
    color: #887854;
`

const Consumers = () => {
    return (
        <div css={Layout}>

        </div>
    )
}

export default Consumers